# NTB Seychelles Procurement Dashboard
# Required to run build.py and the scrapers locally.
# The deployed GitHub Pages site needs no Python at all.

# Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
pdfplumber>=0.10.0
lxml>=4.9.0

# Data + dashboard build
pandas>=2.0.0
openpyxl>=3.1.0
